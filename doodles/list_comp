# name = "Matthew"
# name_list = [letter for letter in name]
# print(name_list)

# import time
# num_list = []
# numbers = [1, 2, 3, 4, 5]
# for n in numbers:
#     num_list.append(n * 2)


# # for i in range(1, 5):
    
# range_list = [i * 2 for i in range (1,11) if i % 2 == 0 ]

# print(range_list)

# names = ['matt', 'tom', 'martha', 'sally']
# # long_names = [name.upper for name in names if len(name) > 4]
# print(long_names)

import random

students = ['Alan', 'Marty', 'Biff', 'Sally', 'Thomas', 'Darren', 'Debbie']
student_scores = {student:random.randint(20,100) for student in students}
passing_students = {student: score for (student, score) in student_scores.items() if score >= 60}
print(student_scores)
print(passing_students)